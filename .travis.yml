language: r

sudo: required
bioc_required: true
warnings_are_errors: true

apt_packages:
 - default-jre
 - default-jdk
 - libnetcdf-dev
 - udunits-bin
 - libudunits2-dev
 
r_binary_packages:
 - RUnit
 - testthat

r_github_packages:
 - jasenfinch/cdkr/rcdk

notifications:
  email:
    on_success: change
    on_failure: change
language: r

sudo: required
bioc_required: true
warnings_are_errors: true

apt_packages:
 - default-jre
 - default-jdk
 
r_binary_packages:
 - RUnit
 - testthat

bioc_packages:
 - survival
 
r_github_packages:
 - jasenfinch/cdkr/rcdk

notifications:
  email:
    on_success: change
    on_failure: change